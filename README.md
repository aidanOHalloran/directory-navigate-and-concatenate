# directory-navigate-and-concatenate
A python script use to search a projects entire structure, allow a user to select certain files within it, and concatenate those files into a single text document.
